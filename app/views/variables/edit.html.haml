- @title = "#{@variable.name_was} Edit"
- @menu_title = "#{@project.name} #{@title}"

- content_for :sidebar, render("projects/sidebar")
- content_for :header do
  = link_to @variable.name_was, [@project, @variable]
  Edit

.dashboard-container= render "form", variable: @variable
