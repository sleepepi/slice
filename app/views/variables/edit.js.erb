$("#variable_popup_container").html("<%=j render 'popup_form' %>");
$("#variable_edit_modal").modal({ dynamic: true });

$("[rel~=popover]").popover({ offset: 10, trigger: 'focus' });
toggleOptions($('#variable_variable_type'));
activateDomainOptions();

loadVariableSortable();

$('#form_grid_variables[data-object~="sortable"]').sortable({ placeholder: "well alert alert-block" });

resetDatePickers();
