.card.mb-3
  %h5.card-header
    Randomizations
    - if project.randomizations_enabled?
      = link_to "ON", toggle_editor_project_path(project, feature: "randomizations", enabled: "0"), method: :patch, remote: true, class: "btn btn-success btn-sm btn-shadow"
    - else
      = link_to "OFF", toggle_editor_project_path(project, feature: "randomizations", enabled: "1"), method: :patch, remote: true, class: "btn btn-light btn-sm btn-shadow"

  .card-body
    .mb-3.d-flex.align-items-center
      .p-3.border-right.text-center{ style: "min-width: 4.5rem;" }= icon("fas", "random", class: "fa-2x")
      .p-3
        - if project.randomizations_enabled?
          = link_to "Manage randomizations", project_randomization_schemes_path(project)
          .small.text-muted
            Setup randomization schemes.
        - else
          .small.text-muted
            Randomizations are disabled for this project.

    .d-flex.align-items-center
      .p-3{ style: "min-width: 4.5rem;" }
      .p-3
        = link_to docs_randomization_schemes_path, class: "text-muted" do
          = icon("fas", "info-circle")
          Learn more...
