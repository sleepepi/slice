Created
=======

    <%= Time.now.strftime("%a, %b %d, %Y at %I:%M %p") %>


Project
=======

    <%= self.project.name %>


Included Files
==============

`-- data_dictionary
    |-- README.txt
    |-- designs.csv
    |-- variables.csv
    `-- domains.csv

Designs
=======

The `designs.csv` file includes section and variable layout information along with any design-specific branching logic.

The `variables.csv` contains the definition of the variable itself, including ranges, variable type, and option domains.

The `domains.csv` contains option domains that include name and values of possible options.


Design Descriptions
===================

<% Design.where(id: sheet_scope.pluck(:design_id)).order(:name).each do |d| %>

<%= d.name %> - Description
<%= "-" * d.name.size %>--------------

    <%= d.description %>


<% end %>
