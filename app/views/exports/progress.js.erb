$("#progress").html("<%=j render 'exports/progress' %>");

<% if @export.total_steps > 0 and @export.total_steps == @export.steps_completed %>
clearInterval(<%= params[:interval] %>);
flashMessage("Export completed successfully.");
<% end %>
