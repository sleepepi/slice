- @title = "New Export"
- @menu_title = "#{@project.name} #{@title}"

- content_for :container_class, "container-fluid"
- content_for :sidebar, render("projects/sidebar")

- content_for :header, @title

= render "form"

.docs-learn-more
  Learn about
  = succeed "." do
    = link_to "exports", docs_exports_path
