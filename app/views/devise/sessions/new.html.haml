- @title = 'Sign in'

- @no_footer = true

.background-gradient

%div{ style: 'position: relative' }
  .landing-cog3
    %span.glyphicon.glyphicon-cog

%p.lead.center Welcome back!

.sign-up-form
  .sign-up-form-title
    Sign in

  = form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'form-vertical' }) do |f|
    .form-group
      = f.label :email, 'Email or Username', class: 'control-label'
      = f.text_field :email, class: 'form-control'

    .form-group
      = f.label :password, class: 'control-label'
      = link_to '(Forgot password?)', new_password_path(resource_name), tabindex: '-1'
      = f.password_field :password, class: 'form-control', autocomplete: 'off'

    .form-group.sign-up-form-button-container
      = f.submit 'Sign in', class: 'btn btn-primary btn-block btn-shadow-small'


- if devise_mapping.confirmable?
  .center{ style: 'margin-bottom:20px' }
    = link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name)

.sign-up-form.center
  New to
  = succeed '?' do
    = ENV['website_name']
  = link_to 'Create an account.', new_user_registration_path

= render 'layouts/simple_footer'
