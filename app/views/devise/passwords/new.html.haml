- @title = 'Forgot your password?'

.sign-up-form
  .sign-up-form-title
    Reset Password

  %p.lead.center
    Enter your email address to receive a password reset email. You may
    = link_to 'contact us', contact_path
    if you don't remember which email you used to register.

  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'form-vertical' }) do |f|
    - if resource.errors.any?
      .callout.callout-danger
        %strong
          Please enter a valid email

        %ul
          - resource.errors.full_messages.each do |msg|
            %li= msg


    .form-group
      = f.label :email, class: 'control-label'
      = f.email_field :email, class: 'form-control'

    .form-group
      = f.submit "Send me reset password instructions", class: 'btn btn-success'
