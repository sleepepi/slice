.form-group
  .form-label-elegant= defined?(key_name) ? key_name : key.to_s.humanize
  .form-control-plaintext
    %span.badge.badge-dark{ style: "background-color: #{object.send(key)};" }
      = object.send(key)
