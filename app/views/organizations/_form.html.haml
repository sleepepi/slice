= form_with(model: organization, local: true) do |form|
  = render "forms/horizontal/text_field", form: form, object: organization, key: :name
  = render "forms/horizontal/submit", form: form, object: organization
