<% is_grid = defined?(grid) %>

<div class="<% unless variable.prepend.blank? %>input-prepend<% end %> <% unless variable.append.blank? %>input-append<% end %>" style="display:inline-block">
  <% unless variable.prepend.blank? %><span class="add-on"><%= variable.prepend %></span><% end %>
  <%= text_field_tag field_name, response, class: "input-#{defined?(control_size) ? control_size : 'large'}", data: { content: variable.description, title: variable.display_name, object: "condition variable-typeahead", variable_id: variable.id }, rel: "tooltip #{'popover' unless variable.description.blank?}", autocomplete: 'off', disabled: disabled, title: variable.display_name, placeholder: variable.display_name %>
  <% unless variable.append.blank? %><span class="add-on"><%= variable.append %></span><% end %>
</div>
