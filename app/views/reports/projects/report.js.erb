$("#report").html("<%=j render 'reports/report' %>");
$("#page").val("<%= params[:page] %>");

loadPeity();

tooltipsReady();

$('.table-responsive-always').scroll( function(){
  setScrollShadow(this)
});
setScrollShadow($('.table-responsive-always'));
