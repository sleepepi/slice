- blank_responses = responses.select(&:blank?)
- valid_responses = responses.reject(&:blank?)

%table.table.table-fixed.table-borderless
  %thead
    %tr
      %th.center N
      %th.center.text-muted Blank
      %th.center Total
  %tbody
    %tr
      %td.center= filter_link Sheet.array_count(valid_responses), @design, variable, ':any', event_id: event_id
      %td.center= filter_link Sheet.array_count(blank_responses), @design, variable, ':blank', event_id: event_id
      %td.center= filter_link Sheet.array_count(responses), @design, nil, nil, event_id: event_id

- if valid_responses.uniq.size <= 50
  = valid_responses.uniq.sort.join(', ')
- else
  .text-muted Individual responses suppressed since the total amount of responses exceeds 50.
