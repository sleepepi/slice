<%= link_to (@project.favorited_by?(current_user) ? '<span class="glyphicon glyphicon-star-empty"></span> Unfavorite'.html_safe : '<span class="glyphicon glyphicon-star"></span> Add to Favorites'.html_safe), favorite_project_path(@project, favorite: @project.favorited_by?(current_user) ? '0' : '1'), remote: true, method: :post, class: "btn btn-xs #{ @project.favorited_by?(current_user) ? 'btn-default' : 'btn-success'}" %>
