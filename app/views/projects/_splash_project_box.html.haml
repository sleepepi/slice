.projects-box{ data: { project_id: project.id } }
  .pull-right.nowrap
    - if sortable
      %i.fa.fa-arrows.handle
    = number_with_delimiter project.subjects.count
    = render "project_preferences/splash_actions", project: project

  = link_to project do
    = project.name
