- %w(hard_minimum soft_minimum soft_maximum hard_maximum).each do |range|
  .form-group.row
    %label.col-md-2.col-form-label= range.titleize
    .col-md-10
      = text_field_tag "variable[#{range}]", @design_option.variable.send(range), class: "form-control", autocomplete: "off"
