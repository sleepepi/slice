Dear <%= @recipient.first_name %>,

The design data import for <%= @design.project.name %> is now complete.

Click the following link to see newly imported data:


    <%= "#{ENV['website_url']}/projects/#{@design.project.to_param}/sheets?design_id=#{@design.id}" %>


Thank you!

<%= ENV['website_name'] %> Mailer
