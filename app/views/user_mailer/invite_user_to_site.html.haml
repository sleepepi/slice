%p{ style: p_style }
  = @site_user.creator.name
  %span{ style: emphasis_style } invited
  you to
  = link_to @site_user.site.project.name, "#{ENV['website_url']}/site-invite/#{@site_user.invite_token}", style: link_style
  as part of the
  = @site_user.site.name
  site.
  Click the following link to accept the invitation:

%p{ style: center_style }
  = link_to 'Accept Invitation', "#{ENV['website_url']}/site-invite/#{@site_user.invite_token}", style: link_style
