%p{ style: p_style }
  Dear
  = succeed "," do
    = @project_user.user.full_name

%p{ style: p_style }
  = @project_user.creator.full_name
  %span{ style: emphasis_style }
    added you as
    - if @project_user.editor?
      an editor
    - else
      a viewer
  on
  = succeed "." do
    = link_to @project_user.project.name, "#{ENV["website_url"]}/projects/#{@project_user.project.to_param}", style: link_style

  - if @project_user.editor?
    You may edit the project and add new designs, variables, and sheets to the project.
  - else
    You may view the project and sheets.
