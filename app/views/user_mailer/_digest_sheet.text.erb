<%= sheet.name %> <%= sheet.user.name if sheet.user %>
     <%= "#{ENV['website_url']}/projects/#{sheet.project.id}/sheets/#{sheet.id}" %> <% if sheet.double_data_entry? %>[Verification Sheet]<% end %>
