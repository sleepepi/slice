<%= @comment.user.name %> COMMENTED on <%= @comment.sheet.name %> on <%=  @comment.sheet.project.name %> located at <%= "#{ENV['website_url']}/projects/#{@comment.sheet.project.to_param}/sheets/#{@comment.sheet.id}" %>.


<% @comment.sheet.comments.order('created_at DESC').limit(5).each do |comment| %>
<%= comment.user.name %> (<%= comment.created_at.strftime("on %b %d, %Y at %I:%M %p") %>):

      <%= comment.description %>


<% end %>


<%= render 'sheet_note', sheet: @comment.sheet %>
