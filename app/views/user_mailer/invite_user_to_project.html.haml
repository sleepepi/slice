= @project_user.creator.name
%span{ style: "padding:5px;background-color:#428bca;color:white" } invited
you to
= succeed '.' do
  = link_to @project_user.project.name, "#{ENV['website_url']}/invite/#{@project_user.invite_token}", style: 'color: #428bca'
%br
%br
Click the following link to accept the invitation:
%br
%br
%center
  = link_to 'Accept Invitation', "#{ENV['website_url']}/invite/#{@project_user.invite_token}", style: 'color: #428bca'
