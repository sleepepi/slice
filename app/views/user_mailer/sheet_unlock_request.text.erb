Dear <%= @editor.first_name %>,

<%= @requestor.name %> has requested that the following sheet be unlocked on project <%= @sheet.project.name %>. You can review the request here:


  <%= "#{ENV['website_url']}/projects/#{@sheet.project.to_param}/sheets/#{@sheet.id}" %>
