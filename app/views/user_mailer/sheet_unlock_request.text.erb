Dear <%= @editor.full_name %>,

<%= @sheet_unlock_request.user.full_name %> has requested that the following sheet be unlocked on <%= @sheet_unlock_request.sheet.project.name %>. You can review the request here:

>>>

<%= @sheet_unlock_request.reason %>

>>>


  <%= "#{ENV["website_url"]}/projects/#{@sheet_unlock_request.sheet.project.to_param}/sheets/#{@sheet_unlock_request.sheet.id}" %>
