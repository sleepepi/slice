Dear <%= @editor.first_name %>,

<%= @sheet_unlock_request.user.name %> has requested that the following sheet be unlocked on project <%= @sheet_unlock_request.sheet.project.name %>. You can review the request here:

>>>

<%= @sheet_unlock_request.reason %>

>>>


  <%= "#{ENV['website_url']}/projects/#{@sheet_unlock_request.sheet.project.to_param}/sheets/#{@sheet_unlock_request.sheet.id}" %>
