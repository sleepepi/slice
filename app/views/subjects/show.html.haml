- @title = @subject.name

= render 'subjects/beta/header'

.row
  .col-sm-2
    = render 'subjects/beta/event_navigation'

  .col-sm-10
    - if @project.events.where(archived: false).count > 0
      = render 'subjects/beta/events'
    - else
      = render 'subjects/beta/sheets'
