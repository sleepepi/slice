<% @notifications.each do |notification| %>
  $("#notification_<%= notification.id %>").html("<%=j render 'notifications/show', notification: notification %>");
<% end %>

<% unless current_user.unread_notifications? %>
  $('.notifications-link').removeClass('notifications-link');
<% end %>
