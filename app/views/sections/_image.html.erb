<% if @design %>
  <% section = @design.sections.find_by_name( @option[:section_name] ) %>
  <% if section and section.image.url %>
    <% full_url = "#{request.script_name}#{section.image.url}" %>
    <%= image_tag full_url, style: 'max-width:100%' %>
  <% end %>
<% end %>
