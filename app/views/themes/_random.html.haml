- if params[:theme].present?
  - @theme = @menu = @body = @body_alt = @footer = params[:theme]
- else
  - @themes = Slice::THEMES.collect(&:second)
  - @theme = @menu = @body = @body_alt = @footer = @themes[rand(@themes.size)]
