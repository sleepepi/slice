- if params[:theme].present?
  - @theme = @menu = @body = @footer = params[:theme]
- else
  - @themes = Slice::THEMES.collect(&:second)
  - @theme = @menu = @body = @footer = @themes[rand(@themes.size)]
