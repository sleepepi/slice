- @title = "#{@block_size_multiplier.name_was} Edit"
- @menu_title = "#{@project.name} #{@title}"

- content_for :sidebar, render("projects/sidebar")
- content_for :header, @block_size_multiplier.name_was
- content_for :breadcrumbs do
  %ol
    %li= link_to "randomization schemes", project_randomization_schemes_path(@project)
    %li= link_to @randomization_scheme.name.downcase, [@project, @randomization_scheme]
    %li= link_to "block size multipliers", project_randomization_scheme_block_size_multipliers_path(@project, @randomization_scheme)
    %li= link_to @block_size_multiplier.name_was.to_s.downcase, [@project, @randomization_scheme, @block_size_multiplier]
    %li.breadcrumb-muted edit

.dashboard-container= render "form"
