- @title = "#{@stratification_factor.name_was} Edit"
- @menu_title = "#{@project.name} #{@title}"
- @breadcrumbs = []
- @breadcrumbs << [@randomization_scheme.name, [@project, @randomization_scheme]]

- content_for :sidebar, render("projects/sidebar")

- content_for :header do
  = link_to @stratification_factor.name_was, [@project, @randomization_scheme, @stratification_factor]
  Edit

.dashboard-container= render "form", stratification_factor: @stratification_factor
