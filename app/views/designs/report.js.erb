$("#report").html("<%= escape_javascript(render("report")) %>");

$.each($('[data-object~="peity"]'), function() {
  $(this).peity($(this).data('method'));
  $(this).show();
});

$(".tooltip").remove();
$("[rel~=tooltip]").tooltip({ trigger: 'hover' });
