- if (params[:user_id].blank? || params[:user_id].to_s == comment.user.id.to_s) && (params[:site_id].blank? || comment.sheet.subject.site_id.to_s == params[:site_id].to_s)
  %a.anchor-top{ name: "c#{current_index}" }
  - unless comment.deleted?
    %div{ id: "comment_#{comment.id}" }
      = render 'comments/show', comment: comment, include_name: '1', number: nil
