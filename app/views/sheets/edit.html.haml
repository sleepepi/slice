- @title = "Edit Sheet: #{@sheet.name}"

- if @subject = @sheet.subject
  = render 'subjects/header'

  = render 'sheets/form_header'
  .markdown-container
    = render 'sheets/form'

- else
  = render 'sheets/form'
