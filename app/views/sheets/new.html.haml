- @title = "New Sheet"

- if @subject = @sheet.subject and beta_enabled?(current_user)
  = render 'subjects/beta/header'
  .row
    .col-sm-2
      = render partial: 'subjects/beta/event_navigation'

    .col-sm-10
      .markdown-container
        = render 'form'

- else
  = render 'form'
