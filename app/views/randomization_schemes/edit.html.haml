- @title = "#{@randomization_scheme.name_was} Edit"
- @menu_title = "#{@project.name} #{@title}"

- content_for :sidebar, render("projects/sidebar")

- content_for :header do
  = link_to @randomization_scheme.name_was, [@project, @randomization_scheme]
  Edit
- content_for :breadcrumbs do
  %ol
    %li= link_to "randomization schemes", project_randomization_schemes_path(@project)
    %li= link_to @randomization_scheme.name_was.downcase, [@project, @randomization_scheme]
    %li.breadcrumb-muted edit

.dashboard-container= render "form"
