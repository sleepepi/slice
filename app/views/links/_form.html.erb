<div class="page-header">
  <h1><%= link_to @link.project.name, @link.project %> &middot; <%= @title %></h1>
</div>

<%= form_for [@link.project, @link], html: { class: 'form-horizontal' } do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, nil, class: 'control-label' %>
    <div class="controls">
      <%= f.text_field :name %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :category, nil, class: 'control-label' %>
    <div class="controls">
      <%= f.text_field :category, autocomplete: "off", data: { provide: "typeahead", source: @link.project.links.pluck(:category).uniq.to_json } %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :url, 'URL', class: 'control-label' %>
    <div class="controls">
      <%= f.text_field :url %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :archived, nil, class: 'control-label' %>
    <div class="controls">
      <%= f.check_box :archived %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, class: 'btn btn-primary' %>
    <%= cancel %>
  </div>
<% end %>
