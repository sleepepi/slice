- @title = 'Links'

= render 'projects/tabbed_menu', links: [['Links', project_links_path(@project)]]

.mini-menu-outer
  .mini-menu-left-fixed
    = render 'projects/menu_mini'
  .mini-menu-right-fluid
    %h2.underline-header
      = @title
      = link_to 'Create Link', new_project_link_path(@project), class: 'btn btn-xs btn-primary'
      .per-page
        = render 'contour/layouts/per_page', per_page: 20, object_count: @links.total_count

    = render 'links/links'

    .jumbotron
      = form_tag project_links_path(@project), method: :get, class: 'form-inline' do
        = hidden_field_tag :order, params[:order]
        = text_field_tag 'search', params[:search], class: 'form-control'
        = submit_tag 'Search', class: 'btn btn-primary', name: nil
