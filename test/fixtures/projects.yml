# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

one:
  name: Project One
  description: MyText
  user: regular
  logo: rails.png
  randomizations_enabled: true
  adverse_events_enabled: true
  blinding_enabled: true
  slug: project-one

two:
  name: Project Two
  description: MyText
  user: regular
  hide_values_on_pdfs: true
  randomizations_enabled: true
  adverse_events_enabled: true
  blinding_enabled: true

three:
  name: Project Three
  description: MyText
  user: admin

four:
  name: Project Four
  description: Only Admin should be on this project
  user: admin

no_sites:
  name: This project has no sites and one design
  description: Use this to test that the regular user can view the design
  user: regular

single_design:
  name: Project With One Design
  description: When creating sheets the single design should always show by default.
  user: regular

empty:
  name: Empty Project
  description: Empty for JSON Import
  user: regular

named_project:
  name: Has a Slug
  slug: has-a-slug
  user: regular

auto_lock:
  name: Auto-lock sheets
  slug: auto-lock-sheets
  user: regular
  auto_lock_sheets: after24hours

default:
  name: Default
  slug: default
  user: regular

api:
  name: API Test
  slug: api-test
  user: regular
  authentication_token: 4bf180956aec927f7a0f01f5

custom_randomizations:
  name: Custom Randomizations
  slug: custom-randomizations
  user: custom_randomizations_editor
  randomizations_enabled: true

engine:
  name: Expressions Engine
  slug: engine
  user: engine_editor

format:
  name: Format All the Values
  slug: format
  user: format_editor

# For new Adverse Event Reviews
aes:
  name: Enhanced Adverse Event Reviews
  slug: aes
  user: aes_owner
  adverse_event_reviews_enabled: true
  blinding_enabled: true
