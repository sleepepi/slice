# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  token: one
  user: valid
  project: one
  subject_event: one

# Created by project editor ("admin"), and relaunched by valid user
two:
  token: two
  user: admin
  project: one
  subject_event: two

grid:
  token: three
  user: valid
  project: one
  subject_event: grid

calculated:
  token: calculated
  user: valid
  project: one
  subject_event: calculated

typeahead:
  token: typeahead
  user: valid
  project: one
  subject_event: typeahead
