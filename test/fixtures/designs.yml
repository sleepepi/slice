one:
  name: Design One
  user: regular
  project: one
  notifications_enabled: true
  survey_slug: design-one

two:
  name: Design Two
  user: regular
  project: two

# This is a blank design.
two_copy:
  name: Design Two Copy
  user: regular
  project: two

# Design with all types of variables used.
all_variable_types:
  name: All Variable Types
  user: regular
  project: one

# Design with no validations or ranges.
has_no_validations:
  name: Design with no validations or ranges
  user: regular
  project: one

# Used to test section and variable reordering.
sections_and_variables:
  name: Sections and Variables
  user: regular
  project: one
  survey_slug: sections-and-variables

# Test file upload and removal.
section_and_file:
  name: Section and File Variable
  user: regular
  project: one

# Test for grid variable saving.
has_grid:
  name: Includes a Grid Variable
  user: regular
  project: one

# Regular user should be able to view and edit this design.
no_sites:
  name: This design is saved on a project with no sites
  user: regular
  project: no_sites

admin_design:
  name: On admin project
  survey_slug: private-design
  user: admin
  project: three

admin_public_design:
  name: On Public Admin Project
  survey_slug: public-survey
  user: admin
  project: three
  publicly_available: true

admin_public_design_with_required_fields:
  name: On Public Admin Project
  survey_slug: public-survey-required-fields
  user: admin
  project: three
  publicly_available: true

admin_public_design_with_redirect:
  name: On Public Admin with Redirect
  survey_slug: public-survey-redirect
  user: admin
  project: three
  publicly_available: true
  redirect_url: 'http://localhost/survey_completed'

single_design:
  name: On single_design project
  user: regular
  project: single_design

one_sheet_entered:
  name: One Sheet of Existing Data
  user: regular
  project: one

weight_and_gender:
  name: Weight and Gender Collected for Reports
  user: regular
  project: one

two_scale_variables:
  name: two_scale_variables
  user: regular
  project: one

contains_single_scale_in_grid:
  name: scale_in_grid
  user: regular
  project: one

checkbox_and_radio:
  name: Checkbox and Radio for Export Test

on_slug_project:
  name: On Project with Slug
  user: regular
  project: named_project
  publicly_available: true

randomize:
  name: Eligible for Randomization
  user: regular
  project: two

auto_lock:
  name: On project that auto-locks sheets
  user: regular
  project: auto_lock
  survey_slug: auto-lock
  publicly_available: true

api_design:
  name: Design on API project
  user: regular
  project: api
  survey_slug: api-design

api_design_two:
  name: Design two on API project
  user: regular
  project: api
  survey_slug: api-design-two

engine_anthropometry:
  name: Anthropometry
  slug: anthropometry
  user: engine_editor
  project: engine

engine_large_number:
  name: Large number
  slug: large-number
  user: engine_editor
  project: engine

engine_small_number:
  name: Small number
  slug: small-number
  user: engine_editor
  project: engine

engine_culture:
  name: Culture
  slug: culture
  user: engine_editor
  project: engine

format_leading_zeros:
  name: Check Leading Zeros in Strings, Integers, and Numerics
  slug: format-numbers
  user: format_editor
  project: format

aes_ae_case_report_form:
  name: Adverse Event Case Report Form
  slug: ae-crf
  user: aes_project_editor
  project: aes

aes_death_adjudication:
  name: Death Adjudication
  slug: death-adjudication
  user: aes_project_editor
  project: aes

aes_heart_failure_adjudication:
  name: Heart Failure Adjudication
  slug: heart-failure-adjudication
  user: aes_project_editor
  project: aes

aes_mild_adjudication:
  name: Mild Adjudication
  slug: mild-adjudication
  user: aes_project_editor
  project: aes

aes_ae_adjudication:
  name: Adverse Event Adjudication
  slug: ae-adjudication
  user: aes_project_editor
  project: aes
