commands:
  01_bundle_clean_and_install:
    command: |
      cd /var/app/ondeck
      bundle clean --force
      bundle config --local force_ruby_platform true
      bundle install --deployment
    ignoreErrors: false
